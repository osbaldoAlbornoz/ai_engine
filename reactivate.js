require('dotenv').config({ path: '.env.local' }); const { createClient } = require('@supabase/supabase-js'); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY); supabase.from('products').update({ status: 'active' }).eq('status', 'inactive').then(({ error }) => { if (error) console.error(error); else console.log('Reactivated inactive products'); });
